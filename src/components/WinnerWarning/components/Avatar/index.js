import React from 'react';
import { AvatarWrapper, AvatarImage, LeaderIcon } from './styles';

const Avatar = ({ auth }) => {
    return (
        <AvatarWrapper>
            <LeaderIcon />
        </AvatarWrapper>
    );
}

export default Avatar;


