import React from 'react';
import PropTypes from 'prop-types';

const CompetitorAlert = ({ auth }) => {
  return <div>dd</div>;
};

CompetitorAlert.propTypes = {
  auth: PropTypes.object.isRequired,
};

export default CompetitorAlert;
